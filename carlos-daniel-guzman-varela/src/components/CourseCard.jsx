const CourseCard = ({ cursos }) => {
    return (
        <div>
            <p>ID: {info.id}</p>
            <h2>Titulo: {info.name}</h2>
            <p>Contexto: {info.age}</p>
            <p>Categoria: {info.email}</p>
            <p>Intervalo: {info.phone}</p>
            <p>Lecciones: {info.address}</p>
            <p>Rango:</p>
            <p>Precio:</p>
            <p>Posicion:</p>
            <p>Estudiante:</p>
            <p>Informacion del curdo:</p>
        </div>
    )
}
export default CourseCard