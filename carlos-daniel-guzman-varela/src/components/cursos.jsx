const cursos = ({ cursos }) => {
    return (
        <div>
            <p>ID Usuario: {info.id}</p>
            <h2>Nombre Usuario: {info.name}</h2>
            <p>Edad Usuario: {info.age}</p>
            <p>Correo Usuario: {info.email}</p>
            <p>Telefono Usuario: {info.phone}</p>
            <p>Direccion Usuario: {info.address}</p>
        </div>
    )
}
export default cursos